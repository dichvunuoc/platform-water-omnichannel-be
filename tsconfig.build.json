{
  "extends": "./tsconfig.json",
  "exclude": [
    "node_modules",
    "test",
    "dist",
    "**/*spec.ts",
    "scripts",
    "// Phase 0 (2026-07-20): ticketing module code-complete nhưng chưa wire + broken (thiếu @nestjs/schedule, BaseAggregateRepository chưa export, TicketRepository.save chưa impl). Exclude khỏi build đến Phase 2 wire lại — lúc đó REMOVE dòng 'src/modules/ticketing' này.",
    "src/modules/ticketing"
  ]
}
