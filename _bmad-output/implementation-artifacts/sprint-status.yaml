# generated: 2026-06-24
# project: OmniCare
# project_key: omnicare
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts
# scope: Wave-1 MVP (Epics 1,2 ✅ + Epics 3,4-mvp,5-mvp,6-mvp,7,8) + Wave-3 G2 (Epics 4-g2,5-g2,6-g2,9)
# source: stories-wave1-mvp.md + stories-wave3-g2.md + Epic 1+2 story files
#
# ═══════════════════════════════════════════════════════════════════
# WAVE ARCHITECTURE — TẠI SAO CHIA WAVE & ỨNG DỤNG MỖI STORY
# ═══════════════════════════════════════════════════════════════════
#
# Chia wave để: (1) demo sớm cho hội đồng, (2) quản lý phụ thuộc
# Ticketing service, (3) ưu tiên tính năng quan trọng trước.
#
# ── WAVE 1 — MVP DEMO (J1 Zalo + J2 VoIP + J3 SLA) ──────────────
# Mục tiêu: demo end-to-end vào FE đã giao, dùng mock + Ticketing stub.
# Không cần DB thật chạy (unit test đủ). Đây là "bản lề" đồ án.
#
#  TICKETING STUB (prerequisite):
#    Stub giả lập Ticketing service — nhận 3 command (TicketCreate,
#    StateChange, Reassign) + phát 4 event (SlaWarning, SlaBreached,
#    TicketClosed, StateChanged). Không có DB thật, trả data tĩnh.
#    → Enables: Epic 3 (SLA render), 6 (CSAT trigger), 8 (dashboard)
#
#  EPIC 1 (✅ DONE — 7 stories, 65 tests):
#    1-1: Nhận webhook Zalo/App/FB/Email → 200-OK ≤200ms + idempotency
#         → Chống timeout Zalo, chống trùng tin khi rớt mạng (FR1-8)
#    1-2: Chuẩn hóa 4 kênh → 1 định dạng OmniMessage chung (FR4)
#         → Agent thấy mọi kênh trong 1 inbox
#    1-3: socket.io push → tin nhắn nảy lên màn hình agent ≤2s (FR12)
#         → Agent không cần F5; tin nhắn real-time
#    1-4: BFF inbox + bootstrap ≤1s (FR9/13/17, NFR2/3)
#         → FE mở app là thấy inbox ngay; filter theo kênh/trạng thái
#    1-5: Agent trả lời → gửi về kênh gốc (Zalo API) (FR5/11)
#         → Khách nhận câu trả lời trên đúng kênh (Zalo/App)
#    1-6: Agent set trạng thái (Available/Busy) + đóng/lưu hội thoại (FR16/18)
#         → Agent quản lý inbox; conversation close ≠ ticket close
#    1-7: AI tag display (mock) — "Vỡ ống 97%" trên màn chat (FR15)
#         → Agent thấy AI phân loại ảnh sự cố ngay trên conversation
#
#  EPIC 2 (✅ DONE — 2 stories):
#    2-1: Resolve zalo_user_id → GlobalCustomerID qua Customer 360 port (FR28/30/31)
#         → Biết ai nhắn tin; fallback nếu khách lạ (không mất tin)
#    2-2: Customer 360 card — hợp đồng, công nợ, tiêu thụ nước (FR29)
#         → Agent thấy ngữ cảnh KH ngay khi mở conversation (bên phải)
#
#  EPIC 3 (needs Ticketing stub):
#    3-1: BFF publish TicketCreateRequested → broker (FR19)
#         → OmniCare CHỈ gửi yêu cầu + link conv↔ticketId khi nhận reply.
#           Việc TẠO ticket thật (FR21-23: assign ID, classify, SLA policy)
#           = Ticketing & SLA service [TKT-SVC], KHÔNG thuộc scope này.
#    3-2: Kanban + SLA countdown + advance stage (FR20/FR60)
#         → Agent/supervisor thấy ticket moving + SLA đang đếm ngược
#    3-3: Render SlaWarning → Kanban chớp đỏ + countdown (FR25)
#         → Supervisor thấy ticket sắp vi phạm SLA trước khi trễ
#
#  EPIC 7:
#    7-1: Dispatch Work Order → Field-team App (FR62)
#         → "Chuyển đội hiện trường" — J1 demo chốt: sự cố → ticket → dispatch
#
#  EPIC 8 (needs Ticketing stub):
#    8-1: Dashboard KPIs (volume + SLA + CSAT) BFF-aggregated (FR53)
#         → Supervisor thấy số liệu real-time trên màn "Điều hành CSKH"
#    8-2: Reassign ticket → TicketReassignRequested (FR54)
#         → Supervisor gán lại ticket khi agent đang bận → cứu SLA
#
#  EPIC 4 MVP slice:
#    4-1: VoIP screen-pop — cuộc gọi đến → pop-up profile KH (FR32/33)
#         → Agent biết ai gọi TRƯỚC khi nhấc máy (J2 demo)
#    4-2: Recording reference + consent (FR35/59)
#         → Lưu vết ghi âm (mock file); thông báo "cuộc gọi có thể được ghi âm"
#
#  EPIC 5 MVP slice:
#    5-1: KB Vietnamese search (FR14/39)
#         → Agent gõ "/" → gợi ý câu trả lời mẫu (FAQ) bằng tiếng Việt
#
#  EPIC 6 MVP slice (needs Ticketing stub):
#    6-1: CSAT survey khi TicketClosed (FR42)
#         → Đóng ticket → gửi khảo sát CSAT → phát CsatSubmitted
#
# ── WAVE 2 — REAL TICKING SERVICE (separate PRD) ─────────────────
# Mục tiêu: thay Ticketing stub bằng service thật (own DB + SLA worker).
# Cutover = config swap (zero rewrite phía Omnichannel — contract-first).
# Không track ở đây (sprint riêng của Ticketing service).
#
#  wave-2-ticketing-cutover:
#    Stub → real Ticketing service. Chạy lại contract test.
#    Unlocks: FR61 (Parent-Incident grouping), FR26/27 (escalation,
#    auto-reopen), real SLA engine.
#
# ── WAVE 3 — G2 DEPTH (deferred by priority) ────────────────────
# Mục tiêu: mở rộng tính năng sau MVP. KHÔNG phụ thuộc wave-2
# (trừ Epic 9 hard-blocked bởi FR61).
#
#  Epic 4-G2 (KHÔNG cần Ticketing — có thể kéo lên sớm):
#    4-3: Ghi âm thật + retention 90 ngày (FR34)
#    4-4: IVR đa nhánh + định tuyến theo kỹ năng/địa bàn (FR36/37)
#    4-5: App callback ≤60s (FR38)
#
#  Epic 5-G2 (KHÔNG cần Ticketing — KB CMS có thể làm sớm):
#    5-2: KB CMS — tác giả → biên tập → phê duyệt → xuất bản (FR40)
#    5-3: Customer self-serve KB search (FR41)
#
#  Epic 6-G2 (soft dep — works with stub):
#    6-2: Multi-channel CSAT + NPS + CES (FR43/44/45)
#    6-3: Closing the loop — <3 sao → callback 24h (FR46)
#    6-4: Customer self-track ticket (FR47) — reads Ticketing state
#    6-5: Deflection measurement ≥30% (FR48)
#
#  Epic 9 (HARD BLOCKED — needs wave-2 real Ticketing FR61):
#    9-1: Outage clustering → Parent-Incident (FR49)
#    9-2: Affected-report view (FR50)
#    9-3: Split mis-merged report (FR51)
#    9-4: GIS pin at intake (FR52)
#
# ═══════════════════════════════════════════════════════════════════

generated: 2026-06-24
project: OmniCare
project_key: omnicare
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # ── Epic 1: Unified Agent Inbox & Multi-Channel Messaging ✅ DONE ──
  epic-1: done   # 7 stories · 65 tests · 29 code-review fixes · ~45 files
  1-1-multi-channel-webhook-ingress-idempotency: done
  1-2-omnimessage-normalization-conversation-store: done
  1-3-realtime-push-gateway-socketio: done
  1-4-unified-inbox-bff-endpoints-bootstrap: done
  1-5-reply-outbound-send: done
  1-6-presence-routing-conversation-close: done
  1-7-ai-insight-display-port: done
  epic-1-retrospective: optional

  # ── Epic 2: Customer Identity & 360° Context ✅ DONE ──
  epic-2: done   # 2 stories · mock Customer 360 (3 VN profiles) + assign handler + 3 BFF endpoints
  2-1-customer-identity-resolution-fallback: done
  2-2-customer-360-card-conversation-view: done
  epic-2-retrospective: optional

  # ── Wave-1 Prerequisite: Ticketing Stub ──
  ticketing-stub: backlog   # Contract-conformant stub: accept TicketCreateRequested/TicketStateChanged/TicketReassignRequested; emit SlaWarning/SlaBreached/TicketClosed/TicketStateChanged. Needed before Epics 3, 6, 8.

  # ── Epic 3: Ticket Interaction & SLA Surfacing (needs Ticketing stub) ──
  epic-3: done   # 3 stories · 15 Epic-3 tests (80 total) · J3 demo flow verified
  3-1-request-ticket-create-link-conversation: done   # FR19 · BFF calls stub + links conv↔ticketId
  3-2-ticket-state-sla-countdown-view-stage-advance: done   # FR20/FR60 · Kanban view + SLA enrichment + advance + reassign
  3-3-consume-render-slawarning: done   # FR25 · SlaWarning subscription in gateway → WS sla.warning push
  epic-3-retrospective: optional

  # ── Epic 7: Field-Incident Dispatch (MVP) ──
  epic-7: done   # 1 story · field-team port + mock adapter + dispatch command + BFF endpoint
  7-1-dispatch-work-order-field-team-app: done   # FR62 · POST /bff/conversations/:id/dispatch-work-order
  epic-7-retrospective: optional

  # ── Epic 8: Supervisor Operations Dashboard (needs Ticketing stub) ──
  epic-8: done   # 2 stories · dashboard KPIs (FR53) + reassign (FR54)
  8-1-operations-dashboard-kpis: done   # FR53 · GET /bff/operations/kpis (volume + SLA + channelMix + CSAT)
  8-2-reassign-ticket: done   # FR54 · POST /bff/tickets/:ticketId/reassign
  epic-8-retrospective: optional

  # ── Epic 4: Voice Call Handling (MVP slice) ──
  epic-4: done   # 2 stories · softphone screen-pop + recording ref (MVP slice)
  4-1-voip-screen-pop: done   # FR32/FR33 · GET /bff/softphone/lookup/:phone + active
  4-2-recording-reference-consent-announcement: done   # FR35/FR59 · GET /bff/calls/:callId/recording
  epic-4-retrospective: optional

  # ── Epic 5: Knowledge Base & Answers (MVP slice) ──
  epic-5: done   # 1 story · KB Vietnamese search (MVP slice)
  5-1-kb-vietnamese-search: done   # FR14/FR39 · GET /bff/kb/search?q=
  epic-5-retrospective: optional

  # ── Epic 6: Customer Feedback & Measurement (MVP slice, needs Ticketing stub) ──
  epic-6: done   # 1 story · CSAT on ticket close (MVP slice)
  6-1-csat-on-ticket-close: done   # FR42 · POST /bff/csat + autoReopen trigger if <3
  epic-6-retrospective: optional

  # ── Wave-3 G2 (deferred by PRIORITY — not all blocked on wave-2) ──
  # Epic 4-G2 + 5-G2: NO Ticketing dependency — deferred by priority only (can pull forward).
  # Epic 6-G2 (6-2/6-3): soft dep on Ticketing events (CSAT trigger) — works with stub.
  # Epic 9 (9-1→9-4): HARD blocked on wave-2 real Ticketing FR61 (parent-grouping).

  # ── Epic 4 G2 (no Ticketing dep — pull-forward candidate) ──
  4-3-call-recording-capture-retention: backlog   # FR34 · recording capture + configurable retention
  4-4-ivr-multi-branch-skill-geo-routing: backlog   # FR36/FR37 · IVR branches + skill/geo routing
  4-5-app-callback-click-to-call: backlog   # FR38/NFR23 · callback queue + ≤60s SLA

  # ── Epic 5 G2 (no Ticketing dep — pull-forward candidate, esp. KB CMS for real content) ──
  5-2-kb-cms-authoring-workflow: backlog   # FR40 · author→edit→approve→publish + versioning
  5-3-customer-self-serve-kb: backlog   # FR41 · public KB search (customer scope)

  # ── Epic 6 G2 (soft dep — works with Ticketing stub via TicketClosed event) ──
  6-2-multichannel-csat-nps-ces: backlog   # FR43/FR44/FR45 · multi-channel survey + NPS scheduler + CES
  6-3-closing-loop-low-scores: backlog   # FR46 · low-score → 24h callback follow-up
  6-4-customer-self-track-ticket: backlog   # FR47 · soft dep: reads ticket state from Ticketing (stub ok)
  6-5-deflection-measurement: backlog   # FR48 · KB/chatbot deflection metric

  # ── Epic 9: Mass-Outage Triage (HARD BLOCKED on wave-2 real Ticketing FR61) ──
  epic-9: backlog
  9-1-outage-detection-clustering: backlog   # FR49/NFR6 · geo+time+type clustering → parent proposal (needs FR61)
  9-2-affected-report-view-parent: backlog   # FR50 · BFF join for affected list (needs FR61)
  9-3-split-mis-merged-report: backlog   # FR51 · split action → Ticketing regroup (needs FR61)
  9-4-gis-pin-at-intake: backlog   # FR52 · geo resolution + GIS pin data
  epic-9-retrospective: optional

  # ── Epic 4/5/6 status note: "partial" — MVP slice (wave-1) + G2 slice (wave-3) ──
  # Mark epic-4/5/6 as done ONLY when BOTH slices are complete.

  # ── Wave-2 Cutover (stub → real Ticketing service) ──
  wave-2-ticketing-cutover: backlog   # Config swap stub→real + rerun contract tests (Arch §5/§10); zero omnichannel-side rewrite
