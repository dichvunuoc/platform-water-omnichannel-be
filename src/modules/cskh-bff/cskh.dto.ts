/**
 * @deprecated — Types + data đã chuyển sang `cskh-fixture.ts` (match FE cũ
 * contract, mock-port verbatim, đa kênh giữ nguyên). File placeholder này giữ
 * để tránh import rời rạc; sẽ xóa khi confirm không còn ai tham chiếu.
 */
export {}
